{
  "name": "cursor-api-easy",
  "version": "0.2.0",
  "description": "Serve your Cursor account as a local OpenAI-compatible API",
  "repository": {
    "type": "git",
    "url": "https://github.com/ZNaiGaomu/cursor-api-easy.git"
  },
  "license": "MIT",
  "type": "module",
  "bin": "./dist/cli.js",
  "scripts": {
    "build": "tsc -p tsconfig.json && node scripts/copy-runtime.mjs",
    "dev": "bun run src/cli.ts",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "@bufbuild/protobuf": "^2.0.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/bun": "^1.3.11",
    "typescript": "^5.9.3"
  }
}
